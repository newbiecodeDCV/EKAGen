name: ekagen
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8.13
  - pip
  - pip:
      # PyTorch + torchvision với CUDA 11.1 runtime
      - torch==1.9.0
      - torchvision==0.10.0
    

      # Core dependencies
      - numpy>=1.20,<1.24
      - pillow>=8.4.0
      - tqdm>=4.66.0
      - pandas>=1.3,<1.6
      - scipy>=1.7,<1.10
      - scikit-learn>=0.24,<1.2
      - pyyaml>=6.0

      # NLP / tokenization
      - nltk>=3.7

      # Image I/O / processing
      - opencv-python>=4.5.5
